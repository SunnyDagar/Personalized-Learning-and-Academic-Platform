# secrets — never commit
.env
*.env
**/config/secrets*
_gw.bin
*.pem
*.key

# deps / build
node_modules/
dist/
__pycache__/
*.pyc
.venv/
venv/

# os / editor
.DS_Store
.idea/
.vscode/

# edge tenant config — never commit
server-edge/config.php

# local comparison snapshot — never publish (duplicate of the live module)
modules/sunny_data_pipeline_copy/
