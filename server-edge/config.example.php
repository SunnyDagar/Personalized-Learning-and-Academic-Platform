<?php
/**
 * Edge configuration template. Copy to config.php and fill in.
 * config.php is git-ignored — never commit a real tenant key.
 */
return [
    // The licensed API this deployment talks to. Provided with your licence.
    'API_BASE'   => 'https://dagarretail.com',

    // Your tenant licence key. Issued per institution; can be revoked at any time,
    // which immediately disables this deployment.
    'TENANT_KEY' => 'CHANGE_ME',
];
