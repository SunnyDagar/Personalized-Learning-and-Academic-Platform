# Portal → API endpoints

Student: `/auth/login`, `/courses/by-professor`, `/documents`, `/chat`, `/quiz/generate`,
`/flashcards/generate`, `/analytics/my-stats|my-trend|my-mastery`, `/assessments`, `/appointments`,
`/notifications`.

Professor: `/courses`, `/documents/upload`, `/assessments/ai-generate`, `/assessments`,
`/assessments/{id}/results`, `/assessments/submissions/{id}`, `/analytics/course/{id}`.
